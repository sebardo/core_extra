
{% block subject %}{{ 'subscription.email.subject' | trans({ '%app_name%': core.name }) }}{% endblock %}

{% block body_text %}
{% autoescape false %}
{{ 'subscription.email.welcome' | trans({ '%app_name%': core.name }) }}

{{ 'subscription.email.message' | trans }}
    
{{ 'subscription.email.thanks' | trans({ '%app_name%': core.name }) }}
{% endautoescape %}
{% endblock %}

{% block body_html %}
{% spaceless %}
    {{ include('CoreBundle:Email:_header.html.twig') }}

    <p style="padding:0 0 20px 0; margin:0;">{{ 'subscription.email.welcome' | trans({ '%app_name%': core.name }) }}</p>

    <p style="padding:0 0 20px 0; margin:0;">{{ 'subscription.email.message' | trans }}</p>

    <p style="padding:0; margin:0;">{{ 'subscription.email.thanks' | trans({ '%app_name%': core.name }) }}</p>

    {{ include('CoreBundle:Email:_footer.html.twig') }}
{% endspaceless %}
{% endblock %}